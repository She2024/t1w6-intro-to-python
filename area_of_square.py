#get the length from the user
length = float(input("Enter the length of a square:"))
#calculate addd store the area in a variable
#print(length)
#calculate and store the area in a variable
area = length * length
#print the calcuated area to the terminal
print(f"The calculated area is {area}")